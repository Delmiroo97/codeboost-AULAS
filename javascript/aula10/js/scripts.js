
// seleciona o elemento pelo atributo do id.
const cabecalho = document.getElementById('cabecalho-js');

// seleciona o elemento pelo atributo da classe.
const cabecalhoClass = document.getElementsByClassName('cabecalho-js');

// seleciona o elemento pelo atributo "." se for classe ou "#" se for Id
const cardBox = document.querySelector('.box');

// seleciona todos os elementos com atributo ".box1"
const cardBox1 = document.querySelectorAll('.box1');

console.log(cardBox1);

// const allDropdown = document.querySelector('.js-dropdown');

